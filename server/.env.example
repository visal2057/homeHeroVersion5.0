NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=homehero_db
DATABASE_USER=postgres
DATABASE_PASSWORD='Maheli@11'

AUTH_SECRET=replace_with_secure_secret
REFRESH_TOKEN_SECRET=replace_with_secure_secret

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email
SMTP_PASSWORD=your_email_password
EMAIL_FROM=HomeHero <noreply@homehero.lk>

# Optional: where Contact Us form submissions are emailed to. Defaults to
# SMTP_USER (the sending mailbox itself) if left unset.
CONTACT_NOTIFICATION_EMAIL=homeherosupport@gmail.com

PASSWORD_RESET_EXPIRY_MINUTES=30
SESSION_EXPIRY_DAYS=7
MEMBERSHIP_BASE_PRICE=4999.00
CLIENT_PLATFORM_FEE_PERCENTAGE=5
MEMBERSHIP_GRACE_DAYS=3

PUBLIC_STORAGE_PATH=storage/public
PRIVATE_STORAGE_PATH=storage/private
